#if JUSTACOMMENT

Window Font MDEF

Keywords: MDEF menu popup Window font geneva

This MDEF can be used to make pop-up menus display in the font and size
of the front window, rather than the system font.  It calls the standard
MDEF to do all the real work, except that it redraws the scrolling arrows. 

#endif