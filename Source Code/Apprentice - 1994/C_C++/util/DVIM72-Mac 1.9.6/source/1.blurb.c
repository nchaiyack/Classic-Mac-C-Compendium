#if justacomment

Previously, the image of the entire page always had to be constructed
in memory before printing.  Now that feature can be turned off, which
makes printing slower but greatly reduces RAM requirements, especially
for high-resolution printers.

All settings take effect immediately.

Settings are now stored in a separate preferences file.

There is an online manual built into the program.

Under MultiFinder or System 7, temporary memory will be used.
Therefore it should rarely be necessary to adjust the memory
partition size.

#endif