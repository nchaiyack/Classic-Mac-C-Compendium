/*
	Definitions to control the code-building in pcmail.c, and the signatures
	assigned to the applications.
*/

#define NOMAIN
#define RMAIL /* if defined we get rmail() else we get lmail() */
#define APPSIGNATURE 'MIS4'
#define PERCENT_ADDRS
