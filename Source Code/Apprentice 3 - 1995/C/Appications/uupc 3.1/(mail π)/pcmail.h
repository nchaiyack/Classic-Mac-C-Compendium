/*	Definitions to control the code-building in pcmail.c*/#define NOMAIN/* off#define RMAIL /* if defined we get rmail() else we get lmail() *//* off#define PERCENT_ADDRS /* define this if your mailhost expects							a%b@c style address on the rmail line */#define APPSIGNATURE 'MIS¢'